{
  "corpus": "fastapi",
  "repo": "https://github.com/fastapi/fastapi.git",
  "keep_subdir": "fastapi",
  "note": "Code-navigation where/how tasks for the context A/B. Ground truth verified against the checkout (paths relative to the sandbox repo root). 'kind' tags whether the answer is directly greppable (parity expected) or semantic (Axil-favored).",
  "tasks": [
    {"id": "fa1", "kind": "semantic", "task": "Where does FastAPI resolve and inject dependencies for a request (the dependency-injection solver)?", "ground_truth": {"file": "fastapi/dependencies/utils.py", "symbol": "solve_dependencies", "notes": "async def solve_dependencies (~598)."}},
    {"id": "fa2", "kind": "semantic", "task": "Where does FastAPI turn a route's return value into a serialized Response?", "ground_truth": {"file": "fastapi/routing.py", "symbol": "serialize_response", "notes": "async def serialize_response (~293); get_request_handler (~367) / run_endpoint_function (~336) acceptable."}},
    {"id": "fa3", "kind": "greppable", "task": "Where is the `APIRouter` class defined?", "ground_truth": {"file": "fastapi/routing.py", "symbol": "APIRouter", "notes": "class APIRouter(routing.Router) (~2210)."}},
    {"id": "fa4", "kind": "semantic", "task": "Where does FastAPI generate the OpenAPI schema for the application?", "ground_truth": {"file": "fastapi/openapi/utils.py", "symbol": "get_openapi", "notes": "def get_openapi (~523)."}},
    {"id": "fa5", "kind": "semantic", "task": "Where does FastAPI validate a request body against its declared fields?", "ground_truth": {"file": "fastapi/dependencies/utils.py", "symbol": "request_body_to_args", "notes": "async def request_body_to_args (~955); _validate_value_with_model_field (~738) acceptable."}},
    {"id": "fa6", "kind": "greppable", "task": "Where is the `Depends` marker defined?", "ground_truth": {"file": "fastapi/param_functions.py", "symbol": "Depends", "notes": "def Depends (~2283) wrapping params.Depends — accept either."}},
    {"id": "fa7", "kind": "semantic", "task": "Where does FastAPI handle a RequestValidationError and build the 422 response?", "ground_truth": {"file": "fastapi/exception_handlers.py", "symbol": "request_validation_exception_handler", "notes": "async def request_validation_exception_handler (~20)."}},
    {"id": "fa8", "kind": "greppable", "task": "Where is the `FastAPI` application class defined?", "ground_truth": {"file": "fastapi/applications.py", "symbol": "FastAPI", "notes": "class FastAPI(Starlette) (~42)."}},
    {"id": "fa9", "kind": "semantic", "task": "Where does FastAPI generate the Swagger UI docs HTML page?", "ground_truth": {"file": "fastapi/openapi/docs.py", "symbol": "get_swagger_ui_html", "notes": "def get_swagger_ui_html (~40)."}},
    {"id": "fa10", "kind": "semantic", "task": "Where does FastAPI run a sync (def, not async def) endpoint function in a threadpool?", "ground_truth": {"file": "fastapi/routing.py", "symbol": "run_endpoint_function", "notes": "run_endpoint_function (~336) via run_in_threadpool."}},
    {"id": "fa11", "kind": "greppable", "task": "Where is FastAPI's `HTTPException` class defined?", "ground_truth": {"file": "fastapi/exceptions.py", "symbol": "HTTPException", "notes": "class HTTPException(StarletteHTTPException) (~17)."}},
    {"id": "fa12", "kind": "semantic", "task": "Where does FastAPI convert arbitrary objects (e.g. Pydantic models) into JSON-compatible data?", "ground_truth": {"file": "fastapi/encoders.py", "symbol": "jsonable_encoder", "notes": "def jsonable_encoder (~129)."}}
  ]
}
